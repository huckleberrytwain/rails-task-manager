<div class="task-card">
  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <%= link_to "Edit", edit_path(@task) %>
  <%= link_to "Delete", destroy_path(@task.id),
        method: :delete,
        data: { confirm: "Are you sure?" } %>
</div>

