<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.label :completed unless @task.new_record? %>
  <%= f.check_box :completed, {}, true, false unless @task.new_record? %>
  <%= f.submit %>
<% end %>
